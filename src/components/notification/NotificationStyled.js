import styled from "styled-components";

const Wrapper = styled.div`
  .notification {
    font-weight: 500;
    font-size: 16px;
    line-height: 1.63;
    color: #212121;
  }
`;

export default Wrapper;
